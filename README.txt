To run the project:
	1.Create a database at your local.(surveychallenge is the preferable name)
	2.Run the SQL scripts that are given at ~DBTable.txt files.
	3.Fallow the Entity framework setup guidelines.